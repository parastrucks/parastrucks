node_modules
dist
# Ignore all env files (incl. backups like ".env .prod .bak") except the example
.env*
!.env.example
.DS_Store
public/Screenshot*.png

.vercel
MIGRATION.md
supabase/.temp/

# TIV source workbooks — commercial market data, deliberately not tracked.
# scripts/tiv/parity-gate.mjs takes the path as an argument; keep the file local.
docs/Market Data*.xlsx
